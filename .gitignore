# Secrets / local config — never commit these
.env
.env.*

# Local SQLite database (transitional; being replaced by Supabase)
planmypickle.db
planmypickle.db-shm
planmypickle.db-wal

# Build artifacts
__debug_bin*
/api
